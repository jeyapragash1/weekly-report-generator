import { PageContainer } from '@/components/layout/page-container';

export function SettingsShellPage() {
  return (
    <PageContainer description="Account and workspace settings shell." title="Settings">
      <div className="rounded-lg border border-dashed border-border bg-card p-8 text-sm text-muted-foreground shadow-sm">
        Settings panels will be connected in a later implementation step.
      </div>
    </PageContainer>
  );
}
