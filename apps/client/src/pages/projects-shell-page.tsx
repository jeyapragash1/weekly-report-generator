import { PageContainer } from '@/components/layout/page-container';

export function ProjectsShellPage() {
  return (
    <PageContainer description="The projects route is registered and protected." title="Projects">
      <div className="rounded-lg border border-dashed border-border bg-card p-8 text-sm text-muted-foreground shadow-sm">
        Project management screens will be added after the application shell.
      </div>
    </PageContainer>
  );
}
